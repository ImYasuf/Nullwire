# Animations

Nullwire centralizes its transition helper in `../Library.lua`, using runtime TweenService when available and direct property assignment as a safe fallback. This keeps loading, window, tab, toggle, and notification transitions lightweight.
