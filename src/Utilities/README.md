# Utilities

Runtime-safe helpers such as service lookup, theme merging, instance construction, tweening, and callback dispatch currently live in `../Library.lua` so the library remains a single-copy ModuleScript.
