# Components

The current release keeps the component factory in `../Library.lua` to make the standalone ModuleScript easy to install. This directory is reserved for future extraction of individual controls without changing the public API.
